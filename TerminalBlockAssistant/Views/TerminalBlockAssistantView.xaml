<UserControl x:Class="TerminalBlockAssistant.Views.TerminalBlockAssistantView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TerminalBlockAssistant.Views"
        mc:Ignorable="d"
        Height="auto" Width="auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="1" >
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding TextBoxes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding _value, Mode=TwoWay}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Grid.Row="0">
                    <Button Content="Individuelle Benennung" Command="{Binding SetIndividualTextBoxesCommand}"/>
                </Border>
                <Border Grid.Column="1" Grid.Row="1">
                    <Button Content="Individuell erstellen" Command="{Binding SetIndividualNamesCommand}"/>
                </Border>
            </Grid>
        </Border>
        <Border Grid.Column="1" Grid.Row="0">
            <ComboBox ItemsSource="{Binding MaterialNames}"
          SelectedItem="{Binding SelectedMaterial, Mode=TwoWay}"/>
        </Border>
        <Border CornerRadius="10" Grid.Column="0" Grid.Row="1">
            <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
        </Border>
        <Border CornerRadius="10" Grid.Column="1" Grid.Row="1">
            <Button Content="Erstellen" Command="{Binding CreateTerminalBlockCommand}" />
        </Border>
    </Grid>
</UserControl>
